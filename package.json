{
	"name": "divmemory",
	"version": "1.0.0",
	"private": true,
	"workspaces": [
		"worker",
		"plugin",
		"cli"
	],
	"scripts": {
		"install:all": "bun install",
		"dev": "bun run --filter @divmemory/worker dev",
		"typecheck": "bun run --filter '*' typecheck",
		"build": "bun run --filter '*' build",
		"test": "bun test && bun run --filter @divmemory/worker test:cf",
		"test:e2e": "playwright test",
		"lint": "biome check .",
		"format": "biome check --write ."
	},
	"devDependencies": {
		"@biomejs/biome": "^2.4.15",
		"@playwright/test": "^1.60.0",
		"@types/node": "^25.9.0",
		"drizzle-orm": "^0.45.2",
		"hono": "^4.12.21",
		"typescript": "^6.0.3",
		"vitest": "^4.1.6"
	},
	"packageManager": "bun@1.3.14"
}
